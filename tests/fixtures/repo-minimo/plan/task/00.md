# Task 00 — Write greeting

## Objective

Leave a `saludo.txt` file at the root of the test repo with a line of known
content; it is the first trivial subtask of the e2e drain.

## Linked context

- [Minimal fixture test plan](../plan.md)

## Constraints

- Only files inside the test repo; no network or credentials.

## Happy path (small example)

After the iteration, `saludo.txt` exists at the root with exactly
`hola fixture`.

## Subtasks

- [ ] Write `saludo.txt` at the root with the line `hola fixture`.
