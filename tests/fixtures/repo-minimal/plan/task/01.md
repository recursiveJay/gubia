# Task 01 — Create output directory

## Objective

Create the `output/` directory with an `echo.txt` file inside; second
trivial subtask of the e2e drain.

## Linked context

- [Minimal fixture test plan](../plan.md)

## Constraints

- Only files inside the test repo; no network or credentials.

## Happy path (small example)

After the iteration `output/echo.txt` exists with the line `fixture echo`.

## Subtasks

- [ ] Create `output/` and write inside it `echo.txt` with the line `fixture echo`.
